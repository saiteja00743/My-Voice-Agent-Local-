# ============================================================
# Offline AI Voice Clone — Requirements
# Python 3.11 | XTTS-v2 | PySide6 | PyTorch CUDA
# ============================================================

# ---- PyTorch (CUDA 11.8 — install via setup.bat for GPU support) ----
# If installing manually for CUDA:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CPU-only:
#   pip install torch torchaudio
torch>=2.1.0
torchaudio>=2.1.0

# ---- Coqui TTS (XTTS-v2 voice cloning engine) ----
TTS>=0.22.0

# ---- GUI Framework ----
PySide6>=6.6.0

# ---- Audio playback ----
sounddevice>=0.4.6
pygame>=2.5.2

# ---- Audio processing ----
numpy>=1.24.0
librosa>=0.10.1
soundfile>=0.12.1
scipy>=1.11.0

# ---- Utilities ----
colorlog>=6.7.0
colorama>=0.4.6

# ---- Required by Coqui TTS internals ----
transformers>=4.35.0
huggingface-hub>=0.19.0
Cython>=3.0.0
coqpit>=0.0.16
trainer>=0.0.36
